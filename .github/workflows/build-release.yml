name: Build and Release

on:
  push:
    tags:
      - 'v*'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read
  actions: read

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "Windows x64"
            os: windows-latest
            artifact_name: blast-dbf-windows-x64.exe
            cross: false

          - name: "Linux x64"
            os: ubuntu-latest
            artifact_name: blast-dbf-linux-x64
            cross: false

          - name: "macOS x64"
            os: macos-15-intel
            artifact_name: blast-dbf-macos-x64
            cross: false

          - name: "macOS ARM64"
            os: macos-latest
            artifact_name: blast-dbf-macos-arm64
            cross: false

          - name: "Linux ARM64"
            os: ubuntu-latest
            artifact_name: blast-dbf-linux-arm64
            cross: true
            cc: aarch64-linux-gnu-gcc
            apt_pkg: gcc-aarch64-linux-gnu

          - name: "Windows ARM64"
            os: windows-11-arm
            artifact_name: blast-dbf-windows-arm64.exe
            cross: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        run: choco install mingw

      - name: Install dependencies (Linux native)
        if: runner.os == 'Linux' && matrix.cross == false
        run: sudo apt-get update && sudo apt-get install -y gcc make

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: command -v make || brew install make

      - name: Install cross-compiler
        if: matrix.cross == true
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc make ${{ matrix.apt_pkg }}

      - name: Build (native)
        if: matrix.cross == false && matrix.name != 'macOS ARM64'
        run: make clean && make

      - name: Build (native macOS ARM64)
        if: matrix.name == 'macOS ARM64'
        run: make clean && make CC=clang CFLAGS="-arch arm64"

      - name: Build (cross)
        if: matrix.cross == true
        run: make clean && make CC=${{ matrix.cc }}

      - name: Test (Unix native)
        if: matrix.cross == false && runner.os != 'Windows'
        run: make test

      - name: Test (Windows native)
        if: matrix.name == 'Windows x64' || matrix.name == 'Windows ARM64'
        shell: bash
        run: ./blast-dbf.exe < sids.dbc | cmp - sids.dbf

      - name: Skip test (cross-compiled)
        if: matrix.cross == true
        run: echo "Skipping test for cross-compiled target ${{ matrix.name }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            blast-dbf
            blast-dbf.exe
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          path: artifacts
          merge-multiple: true

      - name: Create and upload release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete ${{ github.ref_name }} --yes 2>/dev/null || true
          gh release create ${{ github.ref_name }} \
            --title "Release ${{ github.ref_name }}" \
            --notes ""
          find artifacts/ -type f | xargs gh release upload ${{ github.ref_name }}
